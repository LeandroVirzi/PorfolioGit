//guardo en una variable la version de la api.
const API_VERSION = "v1";
const IP_SERVER = "localhost";
const PORT_DB = 27017;//puerto DB

//exportamos la constante.
module.exports = {
    API_VERSION,
    IP_SERVER,
    PORT_DB
};